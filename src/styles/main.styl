/**
 * El Care-Package usa postcss junto con Stylus para brindarle super poderes al
 * CSS, si eres nuevo con el uso de Stylus anímate a aprender!
 * http://stylus-lang.com/
 * https://github.com/postcss/postcss
 * 
 * los plugins de postcss que se incluyen en el Care-Package son:
 * - atoprefixer: Prefijos para las propiedades de CSS que lo necesiten.
 * - rucksack: Paquete de utilidades CSS (http://simplaio.github.io/rucksack/docs/).
 * - lost: Grid System (https://github.com/peterramsing/lost).
 */

/**
 * Nota: puedes incluir cualquier archivo '.css' o '.styl' dentro de la carpeta 
 * 'src/styles' e importarlos directamente en el 'main.styl'. El Care-Package
 * incluye por defecto normalize.styl v3.0.3.
 *
 * Eres libre de modificar los archivos que se encuentran en el directorio
 * 'base' a tu gusto, la idea es mantener tu código organizado y modular, por
 * defecto el Care-Package cuenta con archivos para la declaración de tus
 * base rules, mixins y variables. Se recomienda mantener los `@imports` en el
 * mismo orden.
 */

// Base Imports
@import 'frameworks/normalize'
@import 'base/_base'
@import 'base/_base_mixins'
@import 'base/_base_variables'

// Imports
//@import '../../bower_components/bootstrap/dist/css/bootstrap.css'

.layout
	min-height 100vh
	padding-top 60px
	width 100%
	
.main-header
	display flex
	align-items center
	justify-content center
	background-color lightgrey
	box-shadow 0 0 5px rgba(black,.5)
	height 60px
	position fixed
	width 100%
	
.title
	color white
	font-weight 400
	padding 0
	margin 0
	text-align center
	&--secondary
		@extend .title
		font-size 1.25em
		color black
	&::first-letter
		text-transform uppercase

.subheading
	background linear-gradient(
		lighten(lightgrey,40%),
		white 20%,
		white 80%,
		lighten(lightgrey,40%)
	)







